# Automatic Analysis supports exact file exclusions, not per-rule ignore settings.
# This sample contains placeholder credentials and a deliberate local Home Assistant
# HTTP URL. Runtime configuration and HTTP request implementation remain analyzed.
# https://docs.sonarsource.com/sonarqube-cloud/analyzing-source-code/automatic-analysis
# Reviewed python:S8707 false positives in the frozen shared release helpers.
# prepare_version.py writes validated version text as content to fixed pr-body.md;
# release_versioned.py writes manifest content to a fixed private stage destination.
# Neither content value selects the output path. Automatic Analysis supports exact
# file exclusions here, so retain analysis of the other release helpers.
sonar.exclusions=local_config.example.py,scripts/prepare_version.py,scripts/release_versioned.py
